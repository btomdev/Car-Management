{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <main class="container text-dark mt-5">
        <div class="row">
            <h1 class="h3 mb-3 fw-normal text-center">Compte Admin</h1>
        </div>
        <div class="row mt-5">
            <div class="col d-flex justify-content-center">
                <a href="{{ path('car_index') }}" class="btn btn-dark btn-lg">Voir la liste</a>
            </div>
            <div class="col d-flex justify-content-center">
                <a href="{{ path('car_new') }}" class="btn btn-dark btn-lg">Ajouter une Voiture</a>
            </div>
        </div>
    </main>
{% endblock %}
